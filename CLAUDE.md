# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project state

This repository is in an early, pre-code stage. It currently contains only a README describing the goal:

> mainly things that i want to automate?

There is no build system, source code, dependency manifest, or test suite yet. When code is added, update this file with:
- Build, lint, and test commands (including how to run a single test)
- High-level architecture notes that require reading multiple files to piece together

Until then, there are no framework-specific conventions to follow here — use standard judgment based on whatever language/stack is introduced first.
